import { CheckboxFormatDirective } from './checkbox-format.directive';

describe('CheckboxFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckboxFormatDirective();
    expect(directive).toBeTruthy();
  });
});
